name: Inspect M-Flow header formats

on:
  push:
    paths:
      - "scripts/inspect_mflow_header_formats.py"
      - ".github/workflows/inspect-mflow-header-formats.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python scripts/inspect_mflow_header_formats.py
